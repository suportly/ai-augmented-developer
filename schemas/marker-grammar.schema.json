{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/alairjt/ai-augmented-developer/schemas/marker-grammar.schema.json",
  "title": "Marker Grammar",
  "description": "Canonical grammar for [NEEDS CLARIFICATION:cl-N <question>] markers used in spec and plan documents.",
  "$defs": {
    "cl_marker": {
      "type": "string",
      "pattern": "^\\[NEEDS CLARIFICATION:cl-(?P<id>[1-9][0-9]*)\\s+(?P<question>[^\\]]+)\\]$",
      "description": "A single NEEDS CLARIFICATION marker. Prefix is literal '[NEEDS CLARIFICATION:', followed by 'cl-' and a positive integer (no zero-padding), at least one whitespace separator, question text (no ']'), and closing ']'."
    }
  },
  "const": {
    "marker_pattern": "\\[NEEDS CLARIFICATION:cl-([1-9][0-9]*)\\s+([^\\]]+)\\]"
  }
}
