name: lean
version: 1.0.0
description: >
  Framework-only preset: pipeline skills + constitution, no stack
  opinions. Use as a starting point when no bundled preset fits your
  stack.

variables:
  - name: PROJECT_NAME
    prompt: Project display name
    required: true

provides:
  agent_file: CLAUDE.md
  skills: []                        # no preset-specific skills

adds_articles: []
tightens: []
