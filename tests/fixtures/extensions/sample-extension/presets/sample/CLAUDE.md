# {{PROJECT_NAME}} — sample extension preset

This file is rendered from a sample preset that lives inside the
`sample-extension` test fixture. It exists to prove the install
engine can resolve presets supplied by an extension.
